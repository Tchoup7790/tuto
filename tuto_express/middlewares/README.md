# Example of middlewares with Express.JS
All explanations are written in the code as comments.
## Installation
Install dependencies
```bash
npm install
```
Start the server using the script in the package.json
```bash
npm start
```