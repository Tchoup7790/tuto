<h1 class="text-5xl font-extrabold"><span class="px-2 text-white bg-blue-600 rounded"><%= @category.name %></span> Articles</h1>

<p class="font-bold py-5">Our blog has <%= @articles.length %> <%= @category.name %> articles!</p>

<ul class="flex w-1/2 flex-col flex-1 p-4 gap-5">
    <% @articles.each do |article| %>
        <% unless article.archived? %>
          <li class="w-full font-semibold hover:text-white hover:bg-blue-500 rounded underline underline-offset-3 decoration-4 decoration-blue-500 cursor-pointer">
              <%= link_to article.title, article, class:"py-3 px-5 flex w-full h-full text-xl" %>
          </li>
        <% end %>
    <% end %>
</ul>

<%= link_to "Home", root_path, class:"py-8  text-sm font-semibold leading-6 text-gray-900 hover:underline decoration-blue-500 cursor-pointer" %>
